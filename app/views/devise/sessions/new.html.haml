.AccountPage
  %img{src: "/images/photoa.jpeg", style: "opacity:0.2"}
  .AccountPage__title
    %h2 Log in
    %h5 登録しているユーザーでログイン
    = render "devise/shared/links"
  .AccountPage__form
    = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
      .FormField
        .FormField__label--normal
          = f.label :email
        .FormField__input--normal
          = f.email_field :email,  autocomplete: "off"
        .FormField
          .FormField__label--normal
            = f.label :password
            %i (英数字6文字以上)
          .FormField__input--normal
            = f.password_field :password, autocomplete: "off"
        .actions
          = f.submit "Log in", class: 'Button UserButton'
        .actions1
          = link_to "ゲストログイン(閲覧用)", users_guest_sign_in_path, method: :post, class: 'Button GuestButton'